{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install --legacy-peer-deps && npx ng build --configuration=production"
  },
  "deploy": {
    "startCommand": "cd backend && npm install --legacy-peer-deps && node index.js",
    "healthcheckPath": "/api/tasas",
    "restartPolicyType": "ON_FAILURE"
  }
}
