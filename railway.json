{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install --legacy-peer-deps && NODE_OPTIONS=--max-old-space-size=1024 npx ng build --configuration=production"
  },
  "deploy": {
    "startCommand": "cd backend && npm install --legacy-peer-deps && node index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
